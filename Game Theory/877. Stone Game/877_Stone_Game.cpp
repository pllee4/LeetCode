class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // sort(piles.begin(), piles.end(), greater<int>());
        // int a {};
        // int b {};
        // for (int i = 0; i < piles.size(); ++i) {
        //     if (i % 2 == 0) {
        //         a += piles[i];
        //     } else {
        //         b += piles[i];
        //     }
        // }
        // return a > b;
        return true;
    }
};